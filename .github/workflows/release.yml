name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.21"

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # TAP_GITHUB_TOKEN (a PAT) is required to push to the open-cli-collective/homebrew-tap
      # repository. The default GITHUB_TOKEN only has access to this repository.
      - name: Update Homebrew Tap
        env:
          TAP_GITHUB_TOKEN: ${{ secrets.TAP_GITHUB_TOKEN }}
        run: |
          VERSION="${{ github.ref_name }}"
          VERSION_NUM="${VERSION#v}"

          DARWIN_ARM64_SHA=$(grep "_darwin_arm64.tar.gz" dist/checksums.txt | cut -d' ' -f1)
          DARWIN_AMD64_SHA=$(grep "_darwin_amd64.tar.gz" dist/checksums.txt | cut -d' ' -f1)

          git clone https://x-access-token:${TAP_GITHUB_TOKEN}@github.com/open-cli-collective/homebrew-tap.git
          cd homebrew-tap

          mkdir -p Formula

          cat > Formula/gmail-readonly.rb << 'FORMULAEOF'
          class GmailReadonly < Formula
            desc "Read-only command-line interface for Gmail"
            homepage "https://github.com/open-cli-collective/gmail-ro"
            license "MIT"
            version "VERSION_PLACEHOLDER"

            on_macos do
              on_arm do
                url "https://github.com/open-cli-collective/gmail-ro/releases/download/v#{version}/gmro_v#{version}_darwin_arm64.tar.gz"
                sha256 "DARWIN_ARM64_PLACEHOLDER"
              end
              on_intel do
                url "https://github.com/open-cli-collective/gmail-ro/releases/download/v#{version}/gmro_v#{version}_darwin_amd64.tar.gz"
                sha256 "DARWIN_AMD64_PLACEHOLDER"
              end
            end

            def install
              bin.install "gmro"
            end

            def caveats
              <<~EOS
                To set up gmro:
                1. Create OAuth credentials at https://console.cloud.google.com/
                2. Save credentials to ~/.config/gmail-readonly/credentials.json
                3. Run: gmro search "test" to authenticate

                See https://github.com/open-cli-collective/gmail-ro#setup for detailed instructions.
              EOS
            end

            test do
              assert_match "gmro", shell_output("#{bin}/gmro --help")
            end
          end
          FORMULAEOF

          sed -i "s/VERSION_PLACEHOLDER/${VERSION_NUM}/g" Formula/gmail-readonly.rb
          sed -i "s/DARWIN_ARM64_PLACEHOLDER/${DARWIN_ARM64_SHA}/g" Formula/gmail-readonly.rb
          sed -i "s/DARWIN_AMD64_PLACEHOLDER/${DARWIN_AMD64_SHA}/g" Formula/gmail-readonly.rb

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Update gmail-readonly to ${VERSION_NUM}"
          git push

  chocolatey:
    needs: goreleaser
    runs-on: windows-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Get checksums from release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $version = "${{ github.ref_name }}".TrimStart('v')
          gh release download "${{ github.ref_name }}" --pattern "checksums.txt" --dir .

          $checksums = Get-Content checksums.txt
          $x64Hash = ($checksums | Select-String "windows_amd64.zip").Line.Split()[0]
          $arm64Hash = ($checksums | Select-String "windows_arm64.zip").Line.Split()[0]

          Write-Host "x64 SHA256: $x64Hash"
          Write-Host "arm64 SHA256: $arm64Hash"

          echo "X64_HASH=$x64Hash" >> $env:GITHUB_ENV
          echo "ARM64_HASH=$arm64Hash" >> $env:GITHUB_ENV
          echo "VERSION=$version" >> $env:GITHUB_ENV

      - name: Update version and checksums
        shell: pwsh
        run: |
          # Update version in nuspec
          $nuspec = 'packaging/chocolatey/gmail-readonly.nuspec'
          (Get-Content $nuspec) -replace '<version>0.0.0</version>', "<version>$env:VERSION</version>" | Set-Content $nuspec
          Write-Host "Updated nuspec to version $env:VERSION"

          # Inject checksums into install script
          $script = 'packaging/chocolatey/tools/chocolateyInstall.ps1'
          $content = Get-Content $script -Raw
          $content = $content -replace 'CHECKSUM_AMD64_PLACEHOLDER', $env:X64_HASH
          $content = $content -replace 'CHECKSUM_ARM64_PLACEHOLDER', $env:ARM64_HASH
          Set-Content $script $content
          Write-Host "Injected checksums into install script"

      - name: Pack Chocolatey package
        shell: pwsh
        run: |
          cd packaging/chocolatey
          choco pack
          Write-Host "Created package:"
          Get-ChildItem *.nupkg

      - name: Push to Chocolatey
        shell: pwsh
        run: |
          cd packaging/chocolatey
          $nupkg = (Get-ChildItem *.nupkg).Name
          Write-Host "Pushing $nupkg to Chocolatey..."
          choco push $nupkg --source https://push.chocolatey.org/ --key ${{ secrets.CHOCOLATEY_API_KEY }}
          Write-Host "Successfully pushed to Chocolatey!"

  winget:
    needs: goreleaser
    runs-on: windows-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Install wingetcreate
        shell: pwsh
        run: |
          Invoke-WebRequest https://aka.ms/wingetcreate/latest -OutFile wingetcreate.exe
          Write-Host "Downloaded wingetcreate"

      - name: Check if manifest exists in winget-pkgs
        id: check-manifest
        shell: pwsh
        run: |
          $response = Invoke-WebRequest -Uri "https://api.github.com/repos/microsoft/winget-pkgs/contents/manifests/o/OpenCLICollective/gmail-readonly" -Method Head -SkipHttpErrorCheck
          if ($response.StatusCode -eq 200) {
            Write-Host "Manifest exists - will use wingetcreate update"
            echo "exists=true" >> $env:GITHUB_OUTPUT
          } else {
            Write-Host "Manifest does not exist - will generate and submit new manifests"
            echo "exists=false" >> $env:GITHUB_OUTPUT
          }

      - name: Get checksums from release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $version = "${{ github.ref_name }}".TrimStart('v')
          gh release download "${{ github.ref_name }}" --pattern "checksums.txt" --dir .

          $checksums = Get-Content checksums.txt
          $x64Hash = ($checksums | Select-String "windows_amd64.zip").Line.Split()[0]
          $arm64Hash = ($checksums | Select-String "windows_arm64.zip").Line.Split()[0]

          Write-Host "x64 SHA256: $x64Hash"
          Write-Host "arm64 SHA256: $arm64Hash"

          echo "X64_HASH=$x64Hash" >> $env:GITHUB_ENV
          echo "ARM64_HASH=$arm64Hash" >> $env:GITHUB_ENV
          echo "VERSION=$version" >> $env:GITHUB_ENV

      - name: Update manifest (existing package)
        if: steps.check-manifest.outputs.exists == 'true'
        shell: pwsh
        run: |
          $urlX64 = "https://github.com/open-cli-collective/gmail-ro/releases/download/${{ github.ref_name }}/gmro_v${{ env.VERSION }}_windows_amd64.zip"
          $urlArm64 = "https://github.com/open-cli-collective/gmail-ro/releases/download/${{ github.ref_name }}/gmro_v${{ env.VERSION }}_windows_arm64.zip"

          Write-Host "Updating existing manifest to version $env:VERSION"
          Write-Host "URL x64: $urlX64"
          Write-Host "URL arm64: $urlArm64"
          ./wingetcreate.exe update OpenCLICollective.gmail-readonly --version $env:VERSION --urls "$urlX64|$urlArm64" --submit --token ${{ secrets.WINGET_GITHUB_TOKEN }}

      - name: Create manifest (new package)
        if: steps.check-manifest.outputs.exists == 'false'
        shell: pwsh
        run: |
          $manifestDir = "manifests"
          New-Item -ItemType Directory -Path $manifestDir -Force | Out-Null

          # Update version manifest
          $versionManifest = Get-Content "packaging/winget/OpenCLICollective.gmail-readonly.yaml" -Raw
          $versionManifest = $versionManifest -replace "0\.0\.0", $env:VERSION
          Set-Content "$manifestDir/OpenCLICollective.gmail-readonly.yaml" $versionManifest -Encoding UTF8

          # Update locale manifest
          $localeManifest = Get-Content "packaging/winget/OpenCLICollective.gmail-readonly.locale.en-US.yaml" -Raw
          $localeManifest = $localeManifest -replace "0\.0\.0", $env:VERSION
          Set-Content "$manifestDir/OpenCLICollective.gmail-readonly.locale.en-US.yaml" $localeManifest -Encoding UTF8

          # Update installer manifest with version and checksums
          $installerManifest = Get-Content "packaging/winget/OpenCLICollective.gmail-readonly.installer.yaml" -Raw
          $installerManifest = $installerManifest -replace "0\.0\.0", $env:VERSION
          # Replace x64 checksum (first occurrence)
          $installerManifest = $installerManifest -replace "0{64}", $env:X64_HASH, 1
          # Replace arm64 checksum (second occurrence - after first replacement)
          $installerManifest = $installerManifest -replace "0{64}", $env:ARM64_HASH
          Set-Content "$manifestDir/OpenCLICollective.gmail-readonly.installer.yaml" $installerManifest -Encoding UTF8

          Write-Host "Generated manifests:"
          Get-ChildItem $manifestDir | ForEach-Object { Write-Host "  $_" }

          Write-Host "`nValidating manifests..."
          winget validate --manifest $manifestDir
          if ($LASTEXITCODE -ne 0) {
            Write-Error "Manifest validation failed"
            exit 1
          }
          Write-Host "Validation passed!"

          Write-Host "`nSubmitting new package to Winget..."
          ./wingetcreate.exe submit --token ${{ secrets.WINGET_GITHUB_TOKEN }} $manifestDir

  snap:
    needs: goreleaser
    runs-on: ubuntu-latest
    env:
      TAG: ${{ github.ref_name || inputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TAG }}
          fetch-depth: 0

      - name: Build snap
        uses: snapcore/action-build@v1
        id: build

      - name: Publish to Snapcraft Store
        uses: snapcore/action-publish@v1
        env:
          SNAPCRAFT_STORE_CREDENTIALS: ${{ secrets.SNAPCRAFT_STORE_CREDENTIALS }}
        with:
          snap: ${{ steps.build.outputs.snap }}
          release: stable

  linux-packages:
    needs: goreleaser
    runs-on: ubuntu-latest
    steps:
      - name: Trigger linux-packages repo update
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.LINUX_PACKAGES_DISPATCH_TOKEN }}
          repository: open-cli-collective/linux-packages
          event-type: package-release
          client-payload: |-
            {
              "package": "gmro",
              "version": "${{ github.ref_name }}",
              "repo": "open-cli-collective/gmail-ro"
            }
